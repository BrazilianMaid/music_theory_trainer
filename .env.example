# Circle of Fifths Trainer — Environment Variables
# Never commit real values to this file or to the repository.

# Sentry error monitoring DSN (optional — app works without it, errors just won't be reported)
# Get this from: sentry.io → Your Project → Settings → Client Keys (DSN)
VITE_SENTRY_DSN=
